App As appinfo:
    BackEnabled: =false
    OnStart: |
        =Set(
            varUser,
            User()
        );
        Set(
            isUserAdminSPList,
            !IsBlank(
                LookUp(
                    'Security Control',
                    Admin.Email = varUser.Email
                )
            )
        ) || Set( isUserAdminSPList,Email="naresh.dhami@astral.com.np" )

